# `control-data-link`

Defines the interface between the Control Panel App and the C/C++ network I/O
drivers written by Tamryn.

This crate defines the interface on the Rust side, see [src/lib.rs](./src/lib.rs),
and defines how to compile and link the C code, see [build.rs](./build.rs).
