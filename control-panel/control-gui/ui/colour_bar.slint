export component ColorBar inherits Rectangle {
    in property <image> segment-image;

    height: 40pt;
    border-radius: 3pt;
    clip: true;

    Image {
        width: parent.width;
        height: parent.height;
        source: root.segment-image;
        image-rendering: pixelated;
    }
}
